platform :ios, '13.0'
use_frameworks!

target 'PhotosSearchApp' do

  # Pods for PhotosSearchApp

  target 'PhotosSearchAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotosSearchAppUITests' do
    # Pods for testing
  end

end

plugin 'cocoapods-keys', {
  :project => "PhotosSearchApp",
  :keys => [
    "FlickrAPIKey"
]}
